name: check-unused-artifacts

on: pull_request

jobs:
  check-unused-artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: checkout commits on PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: check for unused artifacts
        run: |

          git diff --name-status HEAD^1 HEAD

